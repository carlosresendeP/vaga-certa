## Metodos de pagamento

chat, vamos fazer os metodos de pagamento, apos clicar no botao de 'fazer upgrade', components/dashboard/settings/BillingSection.tsx   'esqueça o erro de any la foque no pagamento'

objetivo:
-criar os metodos de pagamento
-vamos utilizar o kiwify para fazer o pagamento
- antes de criar quero q vc crie o arquivo de configuração do kiwify com o webhook de pagamento
    Criar um endpoint público do tipo POST com o seguinte caminho:
    /api/webhooks/kiwify
   - Esse endpoint será utilizado para receber os eventos da Kiwify quando assinaturas forem renovadas, canceladas ou atrasadas.
   - Implementar validação de segurança no endpoint. Verificar se o campo token é igual a SEUTOKENAQUI. Caso o token esteja incorreto ou ausente, a requisição deve ser rejeitada com status 403 (não autorizado).
   - Processar o conteúdo do webhook. Utilizar o campo email para localizar o usuário correspondente na tabela Supabase users. Com base no campo evento, aplicar as seguintes lógicas:
    -Se o evento for "assinatura cancelada", "assinatura atrasada" ou qualquer outro que represente perda de acesso, atualizar o plano do usuário para "free"
    -Toda vez que um webhook for processado com sucesso, registrar essas informações nessa tabela para rastreamento.

 -criar o 'Chave de Segurança' que sera o token que o kiwify vai usar para confirmar o pagamento
